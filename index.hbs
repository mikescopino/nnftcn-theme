{{!< default}}

<header class="header center">
  <div class="container">
    <h1>{{@blog.title}}</h1>
    <h2>{{@blog.description}}</h2>
  </div>
</header>

<main id="site-main" class="site-main outer" role="main">
  <div class="container">
    <div class="post-feed">
      {{#foreach posts}}
        {{> "post-card"}}
      {{/foreach}}
    </div>
  </div>
</main>
